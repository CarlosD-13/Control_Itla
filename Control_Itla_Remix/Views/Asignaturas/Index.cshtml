@model IEnumerable<Control_Itla_Remix.Models.Asignatura>

@{
    ViewData["Title"] = "Index";
}

<h2>Asignatura</h2>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Creditos)
            </th>
            <th>
                <a class="btn btn-success" asp-action="Create">Crear nuevo</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Codigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Creditos)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.AsignaturaID">Editar</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.AsignaturaID">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
